export global Styles {
    out property <color> video_color: #24266b;
    out property <length> padding: 12px;
    out property <brush> video_gradient: @linear-gradient(180deg, #dcf4ff 0%, #80d9ff 50%, #dcf4ff 100%);
}
